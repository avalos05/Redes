<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo del LinearLayout (opcional) -->
    <solid android:color="#E9E0E0"/> <!-- O transparente si quieres -->

    <!-- Borde -->
    <stroke
        android:width="0dp"
        android:color="#82B1DB"/> <!-- Cambia el color del borde -->

    <!-- Bordes redondeados -->
    <corners android:radius="12dp"/>

    <!-- Opcional: sombra interna (solo en versiones recientes de Android) -->
    <!-- <padding android:left="2dp" android:top="2dp" android:right="2dp" android:bottom="2dp"/> -->
</shape>